[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
Sphinx = "~=2.2"
sphinx_rtd_theme = "~=0.4.2"
pygments = "~=2.5"
# Uncomment and modify the package name to use autodocs or dynamically get
# the package version
# local-package = {editable = true, path = "."}
recommonmark = "~=0.6"
